'use strict';

// Complete this algo
const minJumps = arr => {
  const numberOfJumpsArr = [0]

  for (let i = 0; i < arr.length; i++) {
    // let curNum = arr[i]
    for (let j = i+1; j < i + curNum; j++) {
      let curIndex = j
      let curVal = array[j]
    }
  }

};

module.exports = minJumps
